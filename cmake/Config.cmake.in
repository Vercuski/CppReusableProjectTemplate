@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
# If myproject_lib ever gains a public dependency (e.g. find_package(fmt)),
# mirror that call here with find_dependency() so consumers resolve it too.

include("${CMAKE_CURRENT_LIST_DIR}/MyProjectTargets.cmake")

check_required_components(MyProject)
